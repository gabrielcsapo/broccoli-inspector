<PaperCard as |card|>
  <card.title as |title|>
    <title.text as |text|>
      <text.headline>Plugins run</text.headline>
      <text.subhead>This shows plugins that were run for this specific build.</text.subhead>
    </title.text>
  </card.title>
  <PaperTabs @stretch={{true}} @borderBottom={{true}} @selected={{this.selectedTab}} @onChange={{fn (mut this.selectedTab)}} as |tabs|>
    <tabs.tab>
      Chart
    </tabs.tab>
    <tabs.tab>
      Table
    </tabs.tab>
  </PaperTabs>
  {{#if (eq this.selectedTab 0)}}
    <C3Chart
      @data={{this.data}}
      @axis={{this.axis}}
    />
  {{else}}
    <Table
      @title=""
      @data={{this.nodes}}
      @empty="No Plugin information"
    />
  {{/if}}
</PaperCard>
